#include <iostream> /* input outputstream */
#include <string.h> /* need this so you can use string declaration */
#include <windows.h> /* the windows header file you need this for windows programs */
using namespace std; /* you need this so you dont have to std:: infront of cout ever time you wan to use it */

int main() {
string monster = "Bear";
cout << monster;
return(0);
}
